pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
        mavenLocal()   // top.fatweb.api-plugin (and the plugin SDK snapshot) are published here
    }
}

rootProject.name = "echo-plugin"
